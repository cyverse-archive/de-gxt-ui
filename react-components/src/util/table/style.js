import Color from "../CyVersePalette";

export default (theme) => ({
    column_heading: {
        backgroundColor: Color.blue,
        position: "sticky",
        top: 0,
        color: Color.white,
        fontSize: 12,
    },
    checkbox_cell: {
        backgroundColor: Color.blue,
        position: "sticky",
        top: 0
    }
});
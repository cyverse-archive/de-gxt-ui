const style = theme => ({
    searchField: {
        padding: '2px',
        '&:hover': {
            background: theme.palette.action.hover,
            cursor: 'pointer'
        }
    }
});

export default style;
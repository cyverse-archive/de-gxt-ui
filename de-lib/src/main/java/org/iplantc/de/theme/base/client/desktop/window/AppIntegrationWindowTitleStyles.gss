.warning {
	color: red;
}

@if (is("gecko") || is("gecko1_8") || is("gecko1_9")) {
	.warning {
		position: absolute;
	}
}

.title {
	float: left;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    width: 50%;
}
